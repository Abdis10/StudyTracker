
function ComingSoon({ title, message }) {
    return (
        <div className="coming-soon">
            <h2>{title}</h2>
            <p>{message}</p>
        </div>
    );
}

export default ComingSoon;